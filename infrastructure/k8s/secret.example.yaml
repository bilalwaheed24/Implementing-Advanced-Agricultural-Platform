# Template only. Never commit a real Secret manifest — populate via `kubectl create secret`
# or an external secrets operator (Development-rules.md §12).
apiVersion: v1
kind: Secret
metadata:
  name: absp-secrets
  namespace: absp
type: Opaque
stringData:
  JWT_SECRET: "CHANGE_ME_generate_a_48_byte_url_safe_random_string"
  ENCRYPTION_KEY: "CHANGE_ME_base64_encoded_32_byte_key"
  DATABASE_URL: "postgresql+psycopg://absp:CHANGE_ME@postgres:5432/absp"
